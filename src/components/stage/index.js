import "./style.css";

export default function Stage() {
  return (
    <div className="stage">
      <label>Start guessing to reveal hints...</label>
      <small>(press [space] or [enter] to reset)</small>
    </div>
  );
}
